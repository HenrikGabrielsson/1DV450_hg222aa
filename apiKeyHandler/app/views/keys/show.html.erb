<% if @key %>
  <p>
    Din nyckel:
    <span id="api_key">
      <%= @key.key %>
    </span>
  </p>

  <p>
    Nyckeln har använts 
    <%= @key.callCount %>
     gånger.
  </p>

  <p>
    <%= link_to "Ta bort nyckel", key_path(id: @key), :method => :delete %>
  </p>

<%
  # om ingen nyckel finns så har den troligtvis blivit borttagen
%>
<% else %>
  <p>Din nyckel har blivit borttagen.</p>
<% end %>